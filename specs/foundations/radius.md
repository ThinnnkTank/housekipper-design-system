# Radius — Foundation

## Intent

`TBD`. Off-grid values 3, 5, 7 acceptable. Use whatever feels right at the primitive level; codify after first 3 primitives ship.

## Rules

- Radii live in `BaseTokens/RadiusTokens.swift`.
- Primitives import `Radius` (SemanticToken alias), never `RadiusToken.rXX` directly.

# SemanticToken — Background

Surface colors. Resolves light/dark automatically via Asset Catalog.

| Token | Maps to | Use |
|---|---|---|
| `BackgroundToken.primary`   | `Colors/paper`  | Default screen background |
| `BackgroundToken.secondary` | `Colors/paper2` | Cards, sheets, recessed surfaces |

**Status:** locked.
**Source:** `SemanticTokens/BackgroundTokens.swift` + `Assets.xcassets/Colors/`.
